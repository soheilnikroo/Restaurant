import React from 'react';
import { View,StyleSheet,Text } from 'react-native';

const SearchBar=()=>{
    return(
        <View style={style.search_display}>sss</View>
    );
};

const style=StyleSheet.create({
    search_display:{
        backgroundColor:'#foeeee',
        width:'80%',
        height:50,
    }
});

export default SearchBar;